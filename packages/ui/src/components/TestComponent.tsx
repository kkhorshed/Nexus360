import React from 'react';

const TestComponent = () => {
  return (
    <div style={{ padding: '20px', background: '#f0f0f0', borderRadius: '8px' }}>
      <h2>Test Component from @nexus360/ui</h2>
      <p>If you can see this, the UI package is working correctly.</p>
    </div>
  );
};

export default TestComponent;
